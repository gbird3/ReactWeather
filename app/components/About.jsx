var React = require('react');

var About = (props) => {
  return(
    <div>
      <h1 className="text-center">About</h1>
      <p>Do you need to know the weather where you are right now? Just search the city where you're at and we can tell you the exact temperature.</p>
    </div>
  );
};

module.exports = About;
